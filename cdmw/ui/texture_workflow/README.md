# Texture Workflow

Owns setup, rule editing, profile selection, progress, compare preview, and
mod-package panels for texture workflows. Pure profile and policy rules belong
in `cdmw/domain/textures/`; execution belongs in services or workers.
